Dependencies:
    debhelper (>= 7.0.50~)
    cmake (>= 2.6)
    libglib2.0-dev (>= 2.32)
    valac (>= 0.16)
    libgee-dev (>= 0.5.3)

Build:
    # cd to the "cerbere" folder
    mkdir build
    cd build
    cmake .. -DCMAKE_INSTALL_PREFIX=/usr
    make
    sudo make install
